# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ML / runtime artifacts — generated at build or run time, never committed.
# model/ holds the trained model.pkl + metadata.json (built inside the image).
# logs/ holds the prediction log (written at runtime to a Docker volume).
model/
logs/

# Environment / secrets
.env

# OS / editor noise
.DS_Store
.idea/
.vscode/

# Note: data/f1_results.csv IS committed on purpose, so the build is reproducible
# offline. Regenerate it any time with: python app/data_prep.py
